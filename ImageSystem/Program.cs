using ImageSystem;

namespace UniversalSystem
{
    class Program
    {

        private static void Main(string[] args)
        {
            ProgramControl programControl = new ProgramControl();
            programControl.StartControl();
        }
    }
}
